global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "llm_autopilot_api"
    static_configs:
      # Use host.docker.internal to scrape the FastAPI app running locally outside docker
      - targets: ["host.docker.internal:8000"]
